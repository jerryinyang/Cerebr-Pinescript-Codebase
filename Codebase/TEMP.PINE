var goShortValid = false
var pendingShort = false

newShortZn = change(shortTop) or change(shortBott)
if newShortZn
    goShortValid := false
    pendingShort := false
    
if shortEntryValid
    goShortValid := true
    pendingShort := true